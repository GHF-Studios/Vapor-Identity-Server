[package]
name = "vapor_identity_server"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["fs", "macros", "net", "rt-multi-thread"] }
